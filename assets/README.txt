Drop your images in this folder using these exact filenames:

  hero.jpg   — cinematic dark image of a port with a truck (used as the hero background)
  fleet.jpg  — a clean photo of a white truck (used in the Fleet section)

If a file is missing, the section still renders with a dark/gray placeholder so the layout never breaks.
